# Nginx config for hash-directory structure
# Add to server block for vw-portal.vercel.app:
location / {
    root /var/www/vw-portal.vercel.app;
    try_files $uri $uri/ @hashdir;
}
location @hashdir {
    root /var/www/vw-portal.vercel.app;
    rewrite ^/([a-z0-9])([a-z0-9])(.+\.html)$ /$1/$1$2/$1$2$3 last;
}